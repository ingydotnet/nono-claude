# nono opts for this specific project
#
# Lines starting with # are comments and ignored.
# Uncomment and edit the options you need.

# --- Filesystem Access ---
# --read <dir>
# --read-file <file>
# --allow <dir>
# --allow-file <file>
# --write <dir>
# --write-file <file>

# Examples for common project types:
#
# Node.js projects:
# --read-file ~/.npmrc
# --allow node_modules
#
# Python projects:
# --read ~/.local/lib
# --read-file ~/.pip/pip.conf
#
# Rust projects:
# --read ~/.cargo
# --read-file ~/.cargo/config.toml

# --- Network ---
# Block all outbound network (offline build):
# --block-net
#
# Allow only specific domains for this project:
# --allow-domain registry.npmjs.org
# --allow-domain pypi.org
# --allow-domain crates.io
#
# Allow dev server ports:
# --listen-port 3000
# --open-port 5173

# --- Credentials ---
# --credential github
# --env-credential <key_name>

# --- Rollback ---
# Enable atomic rollback for this project:
# --rollback
# --rollback-exclude node_modules
# --rollback-exclude .venv
# --rollback-exclude target
