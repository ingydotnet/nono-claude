# nono opts for use in all projects
#
# Lines starting with # are comments and ignored.
# Uncomment and edit the options you need.

# --- Filesystem Access ---
# --read <dir>
# --read-file <file>
# --allow <dir>
# --allow-file <file>
# --write <dir>
# --write-file <file>

# Common directories Claude may need to read:
# --read ~/.ssh
# --read ~/.gitconfig
# --read-file ~/.npmrc
# --read-file ~/.cargo/config.toml

# --- Network ---
# Block all outbound network access:
# --block-net
#
# Restrict network to specific domains:
# --network-profile <profile>
# --allow-domain github.com
# --allow-domain api.github.com
# --allow-domain registry.npmjs.org
# --allow-domain pypi.org
#
# Allow Claude to run dev servers:
# --listen-port 3000
# --listen-port 8080
# --open-port 5173

# --- Credentials ---
# Inject credentials via nono's secure credential proxy:
# --credential github
# --credential npm
#
# Restrict credential access to specific API endpoints:
# --allow-endpoint github:GET:/repos/*/issues
# --allow-endpoint github:POST:/repos/*/pulls
#
# Load API keys from system keystore as env vars:
# --env-credential openai_api_key
# --env-credential anthropic_api_key

# --- GPU ---
# Allow GPU access (needed for ML/AI workloads):
# --allow-gpu

# --- Rollback ---
# Enable atomic rollback snapshots (undo all changes):
# --rollback
# --rollback-exclude node_modules
# --rollback-exclude .cache

# --- Misc ---
# Suppress nono banner output:
# --silent
# Enable runtime capability elevation (interactive prompts):
# --capability-elevation
